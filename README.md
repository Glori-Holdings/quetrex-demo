# quetrex-demo

The Quetrex demo application, built end to end by the Quetrex pipeline.

The repository is deliberately empty of application code — the pipeline builds it.

## Verification

    npm run lint
    npm run typecheck
    npm test
    npm run build
