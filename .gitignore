node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo

# Local JSON store — created at runtime, never committed.
.data/

.env
.env.*
!.env.example

.claude/settings.local.json
.claude/worktrees/
*.log
.DS_Store

# Quetrex pipeline gate artifacts — evidence only, published via a
# dedicated gates branch (git add -f), never committed to the unit branch.
.quetrex/
